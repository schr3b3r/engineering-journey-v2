Address GitHub issues #5, #6, and #7 in Engineering Journey v2 as one cohesive quality/correctness release.

You must read ENGINEERING_STANDARDS.md, CONTEXT.md, features/INDEX.md, the relevant feature specs, implementation, and tests before editing. Preserve the architecture's durable Fulcra records and provenance.

Issue #5: format_narrative_document currently shadows its overall start_time/end_time arguments while iterating paced groups. Preserve the requested overall header bounds and add a multi-month regression test.

Issue #6: parse_narrative_document relies on rollup_/notability_ prefixes, so live UUID IDs in the structurally separate Activity Rollup Records and Notability Signal Records appendix tables are ignored, and validation passes vacuously. Parse IDs structurally from the proper tables, support realistic UUIDs and legacy synthetic IDs, reject expected non-empty provenance when parsing yields zero IDs, and validate completeness/membership in both directions. Add realistic UUID and malformed/missing/mismatched appendix regressions.

Issue #7: restore v1-level narrative specificity, pacing, and technical storytelling. The current period summarization prompt only supplies repo names/counts even though raw GitHubActivityItem title_or_summary/raw_payload evidence exists. Build a grounded, durable, re-runnable synthesis path that carries selected titles/summaries/relevant body context and raw provenance into period summaries and final narrative generation. The document must start with a concise trajectory/themes/focus-shifts overview; have chronological human-readable quarter/month sections; synthesize related cross-repo work where supported; expand notable periods and compress routine/quiet periods; avoid repetitive per-repo templates, exhaustive signals/count dumps, and raw key/value prose; retain traceable claims. Detect missing model credentials before narrative generation (ideally before long pipeline execution), clearly label deterministic fallback as limited and not equivalent. Add a meaningful end-to-end quality regression/evaluation based on a shared activity fixture/window, with assertions/scoring for specificity, chronology, cross-repo synthesis, pacing, repetition, and unsupported claims—not merely file existence or callback invocation.

Use the existing app-only/harness-provider dependency boundary. Do not introduce LLM SDK dependencies into app/. Generation must remain possible from durable Fulcra data without re-querying GitHub solely to rewrite the story. Prefer a focused design that fits existing modules rather than a broad rewrite.

Run the full pytest suite. Update relevant feature documentation, INDEX status if needed, and CONTEXT.md with durable decisions. Commit all app/ changes through git_commit with a clear message.