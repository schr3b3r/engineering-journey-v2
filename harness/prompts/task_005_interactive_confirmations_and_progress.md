Improve the skill's interactive UX so it never feels like it is running without user control.

Requirements from live testing:
1. When an existing gh/GitHub authentication is detected, clearly show account identity and source and let the user choose to use it, authenticate as a different account, or cancel. Never silently accept merely because stdin is non-TTY; non-interactive callers must explicitly opt in only after the user has confirmed. EOF/interrupt must cancel, not default to using the account.
2. Before any backfill writes/API work, show a concise run plan: GitHub identity, exact UTC activity start/end dates, range duration, repository scope, dry-run/write mode, and subsequent pipeline stages. Ask for confirmation with a safe default of No. In a non-interactive environment, print the plan and require an explicit confirmed flag rather than proceeding. Documentation for agents must instruct them to ask the human first and only then pass that flag; remove quickstarts that casually bypass confirmation.
3. Provide thoughtful continuous progress. Print stage transitions and contextual per-repository/subrange state before long calls, plus bounded heartbeat/progress output during repository discovery, GitHub activity fetching, raw ingestion, rollup/notability, model period summarization, narrative generation, and Fulcra upload. Include counts and elapsed time where useful. Flush output promptly. Do not rely on a check that only runs between repositories if one blocking stage may take a long time.

Keep programmatic callback APIs clean and optional. Add tests for existing-account use/change/cancel/non-TTY behavior, plan rendering/confirmation and safe cancellation, and bounded progress callbacks/output through representative long loops. Update SKILL/README/context and run the complete app test suite. Commit app changes through the harness git_commit gate.